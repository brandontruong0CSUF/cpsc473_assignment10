1) Yes, for this simple implementation, the conversion from javascript/jQuery to angularJS was very good. It was easier to manage the data from the view and the model.
2) It may look daunting at first, but with the proper documentation, a front-end developer may actually prefer using this method.
3) For a larger application it may be easier to separate out the views, however the controllers may get convoluted if not managed properly and separated out into their respective controller files.
